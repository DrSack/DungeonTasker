<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DungeonTasker.Views.Settings"
             x:Name="Content">
    
    <ContentPage.Content>
        <StackLayout VerticalOptions="FillAndExpand">
            <Button Text="Delete Account" x:Name="DeleteBtn" HorizontalOptions="CenterAndExpand" Command="{Binding DeleteAccountBtn}" IsEnabled="{Binding CanCloseDelete}"/>
            <Button Text="Logout" HorizontalOptions="CenterAndExpand" Command="{Binding LogoutBtn}" IsEnabled="{Binding CanCloseLogout}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>