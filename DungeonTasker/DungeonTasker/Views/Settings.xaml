<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DungeonTasker.Views.Settings"
             x:Name="Content">
    
    <ContentPage.Content>
        <StackLayout>
            <Button Text="Delete Account" x:Name="DeleteBtn" HorizontalOptions="CenterAndExpand" VerticalOptions="Start" Command="{Binding DeleteAccountBtn}" IsEnabled="{Binding CanCloseDelete}" Padding="100,20"/>
            <Button Text="Logout" Padding="100,20" VerticalOptions="End" HorizontalOptions="CenterAndExpand" Command="{Binding LogoutBtn}" IsEnabled="{Binding CanCloseLogout}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>