<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DungeonTasker.Views.SettingsView"
             x:Name="Content"
             Title="Settings">
    
    <ContentPage.Content>
        <StackLayout VerticalOptions="Start" Margin="0,5,0,0">
            <Button Text="Delete Account" x:Name="DeleteBtn" Command="{Binding DeleteAccountBtn}" IsEnabled="{Binding CanCloseDelete}" BackgroundColor="#F44336" TextColor="White"/>
            <Button Text="Logout" VerticalOptions="Center" Command="{Binding LogoutBtn}" IsEnabled="{Binding CanCloseLogout}" BackgroundColor="Accent" TextColor="Black"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>